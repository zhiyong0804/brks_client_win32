#include "costbutton.h"

CostButton::CostButton(QWidget *parent):
    QPushButton(parent)
{

}

void CostButton::select(bool select)
{
    if(select)
    {
        // 红底 白字
    }
    else
    {
        // 灰di
    }
}
